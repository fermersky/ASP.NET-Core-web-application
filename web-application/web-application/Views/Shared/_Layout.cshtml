<!doctype html>
<html lang="en">
<head>
    <title>@ViewBag.PageTitle</title>
</head>
<body>
    @RenderBody()

    @if(IsSectionDefined("Scripts")) {
        @RenderSection("Scripts", required: true); 
    }
</body>
</html>